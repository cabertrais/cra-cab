import { MomentLocalizedPipe } from './moment-localized.pipe';

describe('MomentLocalizedPipe', () => {
  it('create an instance', () => {
    const pipe = new MomentLocalizedPipe();
    expect(pipe).toBeTruthy();
  });
});
