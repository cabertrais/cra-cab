export interface Mission {
    num: number;        // Identifiant unique de la mission
    libelle: string;    // Libellé de la mission
    couleur: string;    // Couleur associée à la mission (en hexadécimal ou autre format)
}